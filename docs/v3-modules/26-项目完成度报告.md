# 项目完成度报告

> 依据：`scripts/diagnose_cluster.sh` 在 Linux 6.8 + KVM + QEMU 6.2 上的一次完整实测
> （2026-09-16，3 节点 + 7 个阶段全部执行），以及 `origin/main` 上 CI 双平台全绿的构建状态。
> 本文只记录**有证据**的结论；未实测的部分在 §4 明确标注。

## 1. 端到端能力：已打通（实测）

| 阶段 | 结果 | 说明 |
|---|---|---|
| A：VM 启动 + 上传 + exec | ✅ 48s（镜像 31s + 启动 15.1s + 执行 36ms） | `exit_code=1` + `./app: No such file` 是**预期**（`build_cmd=true` 不产出 app） |
| B：guest 网络探测 | ✅ `ROUTE OK` / `HTTP REACHABLE` / `APT-BIN UPDATE OK` | guest 有 DHCP、DNS、出口 |
| C：镜像测速（guest 侧） | ✅ 中科大 9.8 MB/s、deb.debian.org 6.7 MB/s、清华 5.6 MB/s、阿里 1.1 MB/s | 与宿主同量级；阿里/腾讯不稳 |
| D：装 gcc + 编译 + 运行 | ✅ **`project-hello`，exit 0，17.9s** | 含 apt 装 gcc（换源 + 关 deb-src 后从 96s 降到 18s） |
| E：eo-agent 规划命令 | ✅ **`project-hello`，exit 0，57ms** | 热 VM 复用 |
| F：CAS 快照分发 | ✅ **4,004,352 字节从 n1 拉到 n2**，随后 `ready locally` | 见 §2.1 |
| G：隔离对照 | ✅ **reuse 未隔离 / fresh 隔离，均有实测** | 见 §3 |

**e2e 全链路（`eo-agent` → IPC → 路由 → VM → 结果）已经跑通**，这是本项目第一次在真机上
走完「按人类意图提交工程 → 远端沙箱执行 → 回传可读结果」。

## 2. 本轮重点机制：实测证据

### 2.1 快照经 CAS 分发（不是内嵌）

```
cas: blob d9d0fa96…a47 fetched from 12D3KooW…iDEp (4004352 bytes)
project 41dfe9fb…: snapshot d9d0fa96…a47 ready locally (4004352 bytes)
```

- master 侧：`dispatched to peer … (snapshot 3584 bytes, …)` —— 任务消息里只有 hash；
- executor 侧：先查本地 CAS（前几次任务的 3584 字节快照命中，零网络），
  ~4MB 的工作区则触发一次真实拉取。

**这证明「只发 hash + 按需拉取」在真机上成立**，也顺带验证了 `blob_exchange` 超时调整的必要性
（默认 10s 对 4MB 都不够稳，现在是 900s）。

### 2.2 资源策略在启动时可见（不靠猜）

```
Project sandbox policy: enabled=true, vm_mode=Reuse, pool=1, base_image=builtin
qlean worker: mode=Reuse, pool_target=0 (pooling disabled), reuse=true
```

`pool_target=0` 在 reuse 模式下是**正确行为**（复用本来就只留一台，预热无意义），
日志把这件事说清楚了，避免"我配了池怎么没生效"的疑问。

### 2.3 热/冷路径耗时（本次实测）

| 路径 | 耗时 | 构成 |
|---|---|---|
| 冷路径（首次任务） | ~48s | 镜像准备 31s + VM 启动 15.1s + 执行 36ms |
| 冷路径 + 装工具链 | **17.9s**（VM 已启动时） | 换源后的 `apt install gcc` + 编译 + 运行 |
| 热路径（VM 复用 + gcc 已装） | **31-62ms** | 上传 24ms + 编译 37ms + 运行 31ms |
| eo-agent 端到端（热） | **57ms** | 提交 → 结果 |

对照历史（`15-性能重构`）：冷 51.5s / 热 68ms —— 冷路径的「装工具链」部分从不可预期
（曾经 300s 超时）变成**受镜像带宽约束的确定性开销（18s）**。

## 3. 隔离：已用「对照实验」证明（实测）

**机制**：
- `VmMode::{Reuse, Fresh}`（`fresh` = 每任务新 machine = 独立 qcow2 overlay，任务结束即丢弃）；
- VM 池（fresh 模式下空闲时预启动补池，隐藏 ~14s 启动等待）；
- 自定义基础镜像（模板）配置 + 前置换校验。

**实测对照（2026-09-16，同一台机器，同一探针 `/root/eo-isolation-marker`）**：

| 组 | 配置 | 判定 | 解码后的 stdout |
|---|---|---|---|
| G1+G2 | `reuse`（用户默认集群） | **未隔离（预期）** | `MARKER-SURVIVED-reuse-is-not-isolated` |
| G3 | `fresh, pool=0`（生成配置的专用集群，端口 391xx） | **隔离成立** | `MARKER-GONE-isolated` + 两次 `machine discarded (fresh mode)` |

G3 的集群确实是独立的（日志显示它自己发现了 raft id 2/3），所以这两组是**可比的对照**：
同一探针、同一路径，唯一变量是 `project_vm_mode`。**这是「隔离」第一次有真机证据**，
而不只是单测。

同一轮的其他实测数字：

| 路径 | 耗时 |
|---|---|
| reuse 首次任务（含装 gcc） | 19.6s |
| reuse 热任务 | **59ms** |
| fresh 任务（新 machine + 标记写入） | ~14s 启动 + 34ms |

### 3.1 探针曾经错了两次（记录以便复用）

两次都给出了"看起来正确"的结论，但都是空的：

1. 标记写在 **work_dir 里** —— `execute_on_vm` 在两种模式下都会先 `rm -rf work_dir`，
   于是 reuse 也报"已消失"；
2. 探针变量里带了 `M=` 前缀（`ISOLATION_PROBE='M=/root/...'`），展开后 bash 试图执行
   一个名为 `M=/root/...` 的文件，**标记从未写入**，读回必然失败。

两次都是靠**读 executor 打印的 `build=[...]` 数组**发现的。由此固化的两条规则：
- 探针 payload 必须能被肉眼核对 → 新增 `EO_DRY_RUN=1` 打印而不执行；
- 每个结论必须有**能失败的对照**（这次就是 G3 的 fresh 组）。只有单组观察不算证据。

## 4. 功能完成度矩阵

| 模块 | 状态 | 证据 |
|---|---|---|
| 工程执行（qlean VM） | ✅ 可用 | stage A/D/E |
| 快照 CAS 分发 | ✅ 可用 | stage F 的拉取日志 |
| 工具链安装 | ✅ 可用（18s） | stage D `project-hello` |
| eo-agent 启发式规划 | ✅ 可用 | stage E |
| 每任务隔离（fresh） | ✅ **真机验证** | stage G3：`MARKER-GONE-isolated` + `machine discarded (fresh mode)` |
| VM 池 | ⚠️ 实现完成；reuse 下按设计禁用（`pool_target=0`），fresh 下**未验证预热收益** | 单测 |
| 模板镜像 | ⚠️ 配置就绪、**未构造镜像** | `ImageConfig` 契约 + 配置校验单测 |
| Raft：节点注册 + 角色变更 | ✅ 可用 | 网格日志（26 条连接 / 26 个 raft-id 映射） |
| Raft：任务调度 | ❌ **缺失** | 项目任务走 request-response，不经 raft |
| 任务结果持久化 | ❌ 缺失 | 结果在内存 `HashMap`，重启即丢 |
| CAS 回收 / 分块传输 | ❌ 缺失 | — |
| cgroup / 网络限额 | ❌ 缺失 | guest 可自由出网 |
| 轻客户端 master | ⚠️ 本阶段未回归 | — |
| eo-agent LLM 路径 | ⚠️ 未实测（无 key） | — |
| 构建健康 | ✅ CI 双平台绿 | ubuntu + macos：fmt / clippy `-D warnings` / test / release build |

## 5. 测试与验证资产

| 资产 | 内容 |
|---|---|
| 单测 | 71 个（本地）+ ubuntu 上额外 4 个 `cas_fetch`（Linux 门控） |
| 集成测试 | `p2p_discovery` 8 个（含「执行不阻塞事件循环」「无 executor 不静默丢包」回归测试） |
| 端到端脚本 | `verify_agent.sh`、`verify_project_e2e.sh`、`diagnose_cluster.sh`（A–G 七阶段） |
| 环境自检 | `host_network_check.sh`（`--fix` 自动修复）、`preflight.sh`（宿主网络 + **目标平台编译门禁**） |
| CI | GitHub Actions：ubuntu + macos 双平台 |

## 6. 下一步优先级（未做）

1. **ProjectTask 接入 raft 调度**（把 `role_engine`/reroute 从死代码变成可演示能力；
   现在项目任务绕过共识，杀掉执行节点不会有重分配）；
2. **任务结果持久化 + 查询接口**（重启不丢、可追溯）；
3. ~~修好并复跑隔离探针~~ —— 已完成（见 §3）；
4. 预装工具链模板镜像（让 fresh 模式不必每次装工具链）；
5. cgroup / 网络限额（"沙箱"要经得起追问）；
6. CAS 回收 + 大对象分块传输（当前单次响应上限 512MB）。

## 7. 已知的、不该被遗忘的事实

- **`fresh` 模式的代价**：池化只隐藏"启动 14s"，不隐藏"装工具链 18s"（本次 fresh 组没有需要
  工具链的任务，所以"每任务重装"这一点仍**未被实测**，只是由机制推导）；
  两者都解决需要模板镜像（配置已就绪，镜像未造）；
- **本机（macOS）看不到 Linux 门控代码**：`fmt`/`clippy`/`test` 全绿也可能编译不过，
  必须看 CI（ubuntu job）——这一点已经写进 `09-Phase1-bug归档.md`，并连续踩了 5 次；
- **`apt-get update` 会在所有源都失败时仍返回 0**，任何"装成功了吗"的判断都必须基于
  真实 HTTP/文件检查，而不是退出码。
